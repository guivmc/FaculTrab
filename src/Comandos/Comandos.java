package Comandos;

public class Comandos 
{
    private enum commands{MOV, MPY, SUB, ADD, DIV};
    
    public Comandos(String t)
    {
       // if(commands.valueOf(t).equals(commands.ADD)) System.out.println("Hi");
    }
}
